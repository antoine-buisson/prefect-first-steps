{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nimtable-web
  annotations:
    {{- range $key, $value := .Values.ingress.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  rules:
    - host: {{ .Values.ingress.host.hostname }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nimtable-web
                port:
                  number: {{ .Values.nimtableWeb.service.port }}
{{- end }}
